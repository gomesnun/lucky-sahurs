# ---- NUNCA enviar para o GitHub: dados pessoais de quem joga ----
# Saves (savegame_slot1.json...), definicoes, ranking e o lucky_verities_session.json
# (esse ultimo tem o token de login da conta). O projeto nao tem nenhum .json de codigo.
*.json
cloud_cache/
crash_log.txt
.escrita_teste

# ---- ficheiros gerados ----
# Rust (cargo build)
/target/
# escrito pelo GitHub Actions em cada build (com o numero da versao): nunca vai para o repositorio
build_version.py
__pycache__/
*.pyc
build/
dist/
.venv/
*.exe

tools/phases/node_modules/
tools/phases/clean/
